{
  "name": "esbuild-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/index.js --loader:.html=text"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.5",
    "events": "^3.3.0"
  },
  "dependencies": {
    "@unique-nft/accounts": "^0.3.4",
    "@unique-nft/sdk": "^0.2.5"
  }
}
